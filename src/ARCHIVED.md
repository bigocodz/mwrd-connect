# ARCHIVED

This Vite/React/Convex frontend is **archived** as of the Phase 9 cutover.

The replacement monorepo lives in `../frontend/`. Three deployable apps
(admin / client / supplier) plus shared packages — see
`../frontend/README.md` for the new structure.

**Do not modify files here.** Same rationale as `../convex/ARCHIVED.md`:
reference + safety window. Delete in a separate PR after ≥ 2 weeks of
the new stack running in production.
